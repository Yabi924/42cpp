#pragma once

#include <string>
#include <iostream>
#include <fstream>

using std::string;
using std::cout;
using std::endl;
using std::cerr;

bool validDate(string line);
bool validValue(string line);